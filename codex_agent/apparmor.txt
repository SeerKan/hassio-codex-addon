#include <tunables/global>

profile codex_agent flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  file,
  signal (send, receive) peer=*,

  /run.sh ix,
  /sbin/tini ix,
  /bin/** ix,
  /usr/bin/** ix,
  /usr/local/bin/** ix,
  /usr/lib/** rix,
  /usr/local/lib/** rix,
  /opt/codex-agent/** rix,
  /app/** r,

  /data/** rwk,
  /tmp/** rwk,
  /run/** rwk,
  /dev/null rw,
  /dev/urandom r,
  /etc/ssl/** r,
  /etc/hosts r,
  /etc/resolv.conf r,
  /etc/passwd r,
  /etc/group r,

  /homeassistant/** rwk,
  /config/** rwk,
  /addon_configs/** rwk,
  /addons/** rwk,
  /share/** rwk,
  /media/** rwk,
  /ssl/** rwk,
}
