[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["codex_agent/src"]

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["codex_agent/src", "tests", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["B008"]
